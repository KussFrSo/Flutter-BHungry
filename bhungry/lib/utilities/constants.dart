import 'package:flutter/material.dart';

const primaryColor = Color.fromARGB(255, 246, 222, 4);
const secondaryColor = Color.fromARGB(255, 255, 174, 72);
const inputForm = Color.fromARGB(30, 0, 0, 0);

const gradient = LinearGradient(
    begin: Alignment.centerLeft,
    end: Alignment.centerRight,
    colors: [Color(0xffF3953D), Color(0xffF6DE04)]);

const gradientInv = LinearGradient(
    begin: Alignment.centerLeft,
    end: Alignment.centerRight,
    colors: [Color(0xffF6DE04), Color(0xffF3953D)]);

const gradientvertical = LinearGradient(
    begin: Alignment.topRight,
    end: Alignment.bottomLeft,
    colors: [Color(0xffF3953D), Color(0xffF6DE04)]);
